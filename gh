name: Daily Iching Stock Analysis

on:
  workflow_dispatch:
  push:

permissions:
  contents: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - run: echo "first run to register workflow"
